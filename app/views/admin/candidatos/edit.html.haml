- @forview = "EDIT"
= render :partial=>"breadcrumbs"

.mantform
  %h2
    Editar 
    = Candidato.human_name

  - semantic_form_for @candidato, :url => admin_candidato_path(@cantidato) do |form|
    = form.semantic_errors
    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(admin_candidatos_path)
